# Agentes verification

**Verdict**: FAIL
**Profile**: ui
**Diff range**: 56fe7e3..HEAD (HEAD = 7148c0c; feature commits d89aafe, cad7eb0, 5009b72)
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

Note on scope: the working tree carries unrelated, already-in-progress changes (`.gitignore`, `.specs/STATE.md`, `.specs/features/web-frontend/*`, `features.json`'s pre-existing entries, `src/Api/Features/Authorization/GetRole.cs`, `src/Api/openapi.json`, `src/Api/test-results/.last-run.json`, `src/web/e2e/users.spec.ts`, `src/web/src/app/architecture.spec.ts`) from a different task (`web-frontend`/`auth-cookie-contract` reconciliation). None of it touches `src/Api/Features/Ai/**`, `src/web/src/app/features/ai/**`, or `.specs/features/agentes/**`, and none of it is attributed to this verification. `git status --porcelain` before and after this run is identical except for two new untracked files created by an unrelated, concurrent process during the run (`.specs/features/conversas-agente/checks.md`, `.specs/features/observabilidade-agente/checks.md` — different features, not touched by anything in this report).

Step 5 ("walk the flow with the user") is skipped: no user channel is available in this run, consistent with prior rounds in this repo.

## Binding sources

| Source | Opened | Contradiction | Uncovered |
| --- | --- | --- | --- |
| `src/Api/Features/Ai/*` (`ILlmService`, `StubLlmService`, `AgentLoop`, `ChatAi`, tools, `AiContracts.cs`) | yes - read in full (`Agent.cs`, `AgentContracts.cs`, `AgentFile.cs`, `AiModule.cs`, `AiPermissions.cs`, `ChatAi.cs`, `CreateAgent.cs`/`ListAgents.cs`/`GetAgent.cs`/`UpdateAgent.cs`/`DeactivateAgent.cs`, `CreateAgentFile.cs`/`ListAgentFiles.cs`/`GetAgentFile.cs`/`DeleteAgentFile.cs`, `AgentFileTools.cs`, `AgentLoop.cs`, `ToolRegistry.cs`, `LlmServiceResolver.cs`, `StubLlmService.cs`, `OpenRouterLlmService.cs`, `MicrosoftAgentFrameworkLlmService.cs`, `ToolAuthorization.cs`) | none | see Coverage - several HTTP-boundary status codes this module's own endpoints declare (`Produces`/`ProducesProblem`) have no test that reaches them |
| `src/Api/Features/Tenants/{Create,List,Get,Update,Deactivate}Tenant.cs` (CRUD to mirror) | yes - read in full | none - `CreateAgent`/`ListAgents`/`GetAgent`/`UpdateAgent`/`DeactivateAgent` mirror the Tenants shape field-for-field (validator, handler, `IUnitOfWork.SaveChangesAsync`, `RequireAuthorization`, `Produces`/`ProducesProblem`) | - |
| `docs/security/RBAC_MATRIX.md`, `PermissionCatalog`, `SecurityPolicies` (`Api.Shared`) | yes | none - matrix's AI section (`AiAgentsRead`/`AiAgentsManage`, `Authenticated` for chat, `EnableAI` on every row) matches `AiModule.AddAiModulePolicies`, `features.json`, and `PermissionCatalog.cs:20-21` (`AiPermissions.AgentRead`/`AgentManage` seeded) exactly | - |
| `src/Api/Shared/Infrastructure.cs` (`AppDbContext`, I5) | yes | none - `AppDbContext.OnModelCreating` uses `ApplyConfigurationsFromAssembly`, which auto-discovers `AgentConfiguration`/`AgentFileConfiguration` in the same context; no second `DbContext`; one migration exists at `src/Api/Shared/Migrations/20260915193500_AddAiAgents.cs` | - |
| binding screens `tenants-list`, `tenant-form`, `chat`, `list-state`, `confirm` | yes - read `tenants-list.ts`, `tenant-form.ts` (via agent-form comparison), `chat.ts` (pre-agentes shape inferred from current `chat.ts`), `list-state.ts`, `confirm.ts`, plus the new `agents-list.ts`/`agent-form.ts`/`chat.ts` | none on structure/arrangement: `agents-list.ts` reproduces the Tenants arrangement (`header` h1+action, `mat-form-field` Pesquisar, `app-list-state`, `mat-table`+`matSort`, `mat-paginator`, confirmed by DOM-order assertions in `agents-list.spec.ts:68-84`); chat's picker sits above `chat-history` at full card width (`chat.ts:39-50`, `.agent-picker{width:100%}`, asserted `chat.spec.ts:130-142`) | `confirm.ts`'s dialog `title`/`confirmLabel` copy (AC22: `'Desativar agente'`/`'Desativar'`) is never asserted by any test - `stubConfirm` (`src/web/src/testing.ts:180-182`) replaces `ConfirmService.ask` wholesale and ignores its arguments, so the actual dialog copy passed by `agents-list.ts:177-181` is unverified (see C22) |

## Checks

| Check | Claim | Proof run | Evidence | Result |
| --- | --- | --- | --- | --- |
| C1 | Create persists + `201` w/ shape | `dotnet test tests/Api.Tests --filter FullyQualifiedName~CreateAgentTests.Handle_ShouldCreateAgent_WhenInputIsValid` - exit 0, 1 passed | `CreateAgentTests.cs:54-58` `Assert.Equal("Support", result.Name); ... Assert.NotEqual(Guid.Empty, result.AgentId);` - calls `CreateAgentHandler.Handle` directly, never through `TestWebApplicationFactory`; no proof anywhere asserts HTTP `201` for a plain create | **FAIL - level gap** (claim names `201`; proof sits below the boundary) |
| C2 | List defaults + tenant filter + order | `dotnet test tests/Api.Tests --filter FullyQualifiedName~ListAgentsTests.Handle_ShouldReturnTenantAgents_OrderedByCreatedAtDesc` - exit 0, 1 passed | `ListAgentsTests.cs:281-284` `Assert.Equal(2, page.TotalCount); Assert.Equal(1, page.PageNumber); Assert.Equal(20, page.PageSize); Assert.Equal("Second", page.Data[0].Name);` | PASS (no status code named in AC2 text); precision gap: `Id` tie-break on equal `createdAt` untested |
| C3 | Duplicate name -> `409` | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~CreateAgentTests.Handle_ShouldThrow_WhenNameAlreadyExists\|FullyQualifiedName~CreateAgentTests.Post_ShouldReturn409_WhenNameIsDuplicate"` - exit 0, 2 passed | `CreateAgentTests.cs:73-74` `Assert.ThrowsAsync<BusinessRuleException>` + `CreateAgentTests.cs:127-129` `Assert.Equal(HttpStatusCode.Conflict, second.StatusCode); Assert.Equal("Business rule violation", problem?.Title);` | PASS - crosses boundary; killed by fault F1 |
| C4 | Update mutates row + `200` | `dotnet test tests/Api.Tests --filter FullyQualifiedName~UpdateAgentTests.Handle_ShouldMutateSameRow_WhenInputIsValid` - exit 0, 1 passed | `UpdateAgentTests.cs:331-334` `Assert.Equal(created.AgentId, updated.AgentId); Assert.Equal("Support desk", reloaded.Name); ...` - handler-level only; no test asserts `HttpStatusCode.OK` for a successful `PUT` anywhere in the suite | **FAIL - level gap** (AC4 text: "...responder `200`") |
| C5 | Deactivate (not last) -> `204` + `isActive:false` | `dotnet test tests/Api.Tests --filter FullyQualifiedName~DeactivateAgentTests.Handle_ShouldDeactivate_WhenNotLastActive` - exit 0, 1 passed | `DeactivateAgentTests.cs:433` `Assert.False(reloaded.IsActive);` - handler-level; no test asserts `HttpStatusCode.NoContent` for `DELETE /api/v1/ai/agents/{agentId}` anywhere | **FAIL - level gap** (AC5 text: "...responder `204`") |
| C6 | Unknown id -> `404` "Not found" (GET/PUT/DELETE) | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~GetAgentTests.Handle_ShouldThrow_WhenAgentDoesNotExist\|FullyQualifiedName~UpdateAgentTests.Handle_ShouldThrow_WhenAgentDoesNotExist\|FullyQualifiedName~DeactivateAgentTests.Handle_ShouldThrow_WhenAgentDoesNotExist"` - flaky, see below | `GetAgentTests.cs:299-300`, `UpdateAgentTests.cs:406-409`, `DeactivateAgentTests.cs:462-463` - all three assert only `Assert.ThrowsAsync<NotFoundException>` on the handler; no HTTP-level `404` test exists for `GET`/`PUT`/`DELETE /api/v1/ai/agents/{unknown}` | **FAIL - level gap**, and the `UpdateAgentTests` proof is additionally **flaky** (see below) |
| C7 | Unknown tool -> `400` | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~CreateAgentTests.Validator_ShouldFail_WhenToolNameIsUnknown\|FullyQualifiedName~CreateAgentTests.Post_ShouldReturn400_WhenToolNameIsUnknown"` - exit 0, 2 passed | `CreateAgentTests.cs:145-147` `Assert.Equal(HttpStatusCode.BadRequest, response.StatusCode); Assert.Equal("Validation failed", problem?.Title);` | PASS |
| C8 | `EnableAI=false` -> `404` on all verbs | `dotnet test tests/Api.Tests --filter FullyQualifiedName~CreateAgentTests.Agents_ShouldReturn404_WhenEnableAiIsFalse` - exit 0, 1 passed | `CreateAgentTests.cs:168-171` `Assert.Equal(HttpStatusCode.NotFound, get.StatusCode); Assert.Equal(HttpStatusCode.NotFound, post.StatusCode); ... Assert.Equal("Feature disabled", problem?.Title);` | PASS |
| C9 | `403` without read/manage | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~CreateAgentTests.Get_ShouldReturn403_WhenCallerLacksReadPermission\|FullyQualifiedName~CreateAgentTests.Post_ShouldReturn403_WhenCallerLacksManagePermission"` - exit 0, 2 passed | `CreateAgentTests.cs:192`, `:207` `Assert.Equal(HttpStatusCode.Forbidden, response.StatusCode);` | PASS |
| C10 | Seed: one active default agent (dev + `CreateTenant`) | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~CreateAgentTests.Seed_ShouldCreateDefaultAgent_ForDevTenant\|FullyQualifiedName~CreateAgentTests.CreateTenant_ShouldSeedDefaultAgent"` - exit 0, 2 passed | `CreateAgentTests.cs:88-93` `Assert.Equal(1, page.TotalCount); ...Assert.Equal(AgentSystemPrompt.Text, seed.Instructions); Assert.Equal(AgentToolNames.DefaultSeed, seed.ToolNames);` + `:111-113` | PASS; production wiring confirmed structurally at `AppSeeder.cs:16-18` (`TenantSeeder`→`AgentSeeder`→`DevBootstrapSeeder`) and `CreateTenant.cs:55-56` |
| C11 | Deactivate last active -> `409`, stays active | `dotnet test tests/Api.Tests --filter FullyQualifiedName~DeactivateAgentTests.Handle_ShouldThrow_WhenLastActiveAgent` - exit 0, 1 passed | `DeactivateAgentTests.cs:449-450` `Assert.ThrowsAsync<BusinessRuleException>` - handler-level only, no HTTP `409` test for this path, and the test never reloads the agent to confirm it "stays active" | **FAIL - level gap** (AC11 text: "...responder `409`..."); checks.md's own Test-policy Evidence row admits this ("proven at handler level (C3, C11) **and** 409 at HTTP (C3)" - C11 is left out of the HTTP half) |
| C12 | Chat with `agentId` uses that agent's instructions+allowlist | `dotnet test tests/Api.Tests --filter FullyQualifiedName~ChatAiHandlerTests.Handle_ShouldUseAgentAllowlist_WhenAgentIdIsProvided` - exit 0, 1 passed | `ChatAiHandlerTests.cs:71-74` `Assert.Equal(agent.Instructions, llm.Last.SystemPrompt); Assert.Equal(new[]{AgentToolNames.GetTenantInfo}, llm.Last.Tools?.Select(t=>t.Name).ToArray());` | PASS - killed by fault F3 |
| C13 | Omit `agentId` -> seed default, `200` w/ `reply`/`iterationsUsed` | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~ChatAiHandlerTests.Handle_ShouldReturnReply_WhenMessageIsValid\|FullyQualifiedName~ChatAiTests.ChatAi_ShouldReturn200_WhenEnableAiIsTrueAndAuthenticated"` - exit 0, 2 passed | `ChatAiHandlerTests.cs:32-33` (`result.Reply`/`IterationsUsed`) + `ChatAiTests.cs:68` `Assert.Equal(HttpStatusCode.OK, response.StatusCode);` | PASS - one at boundary, one at own level |
| C14 | Unknown/inactive `agentId` -> `404`, no silent default | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~ChatAiHandlerTests.Handle_ShouldThrow_WhenAgentIdIsUnknown\|FullyQualifiedName~ChatAiHandlerTests.Handle_ShouldThrow_WhenAgentIsInactive"` - exit 0, 2 passed | `ChatAiHandlerTests.cs:86-87`, `:106-107` - both `Assert.ThrowsAsync<NotFoundException>` at handler level; no `ChatAiTests` HTTP test covers a `404` for a bad/inactive `agentId` (only the `EnableAI=false` `404`, a different path, is HTTP-tested) | **FAIL - level gap** |
| C15 | Tool outside allowlist not executed | `dotnet test tests/Api.Tests --filter FullyQualifiedName~ChatAiHandlerTests.Handle_ShouldNotExecuteTool_OutsideAllowlist` - exit 0, 1 passed | `ChatAiHandlerTests.cs:113-130` - registers `ThrowingUserDirectory` (throws if `get_users_summary` runs), then asserts `Assert.False(string.IsNullOrWhiteSpace(result.Reply))` with no exception | PASS (indirect: absence-of-throw proxy, not the literal `{"error":...}` shape named in AC15, but settles the behavioural claim) |
| C16 | Chat stays `Authenticated` | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~ChatAiTests.ChatAi_ShouldReturn401_WhenNotAuthenticated\|FullyQualifiedName~ChatAiTests.ChatAi_ShouldReturn200_WhenEnableAiIsTrueAndAuthenticated"` - exit 0, 2 passed | `ChatAiTests.cs:50`, `:68` | PASS |
| C17 | `EnableAI=false` -> `404` on chat | `dotnet test tests/Api.Tests --filter FullyQualifiedName~ChatAiTests.ChatAi_ShouldReturn404_WhenEnableAiIsFalse` - exit 0, 1 passed | `ChatAiTests.cs:33-35` | PASS |
| C18 | History accepted, no server persistence | `dotnet test tests/Api.Tests --filter FullyQualifiedName~ChatAiHandlerTests.Handle_ShouldAcceptHistory_WithoutPersistingThreads` - exit 0, 1 passed | `ChatAiHandlerTests.cs:146` `Assert.Equal(0, db.Set<AgentFile>().Count());` | PASS; precision gap - asserts an unrelated table (`AgentFile`) stayed empty as a proxy; there is no thread/conversation entity in the model at all (confirmed by reading `ChatAiHandler.Handle`, which never writes one), so the claim holds structurally even though the cited assertion is a loose proxy |
| C19 | Empty state copy + CTA | `cd src/web && npx ng test --no-watch --include src/app/features/ai/agents-list.spec.ts --filter "estado vazio"` - exit 0, 1 passed | `agents-list.spec.ts:60-65` `expect(text(fixture,'list-empty')).toContain('Nenhum agente'); ... toContain('/ai/agents/new')` | PASS |
| C20 | Loading state | `cd src/web && npx ng test --no-watch --include src/app/shared/list-state.spec.ts --filter "estado de carregamento: agents"` - **exit 0, 0 tests matched (all 15 skipped)** | Actual test name is `estado de carregamento: 'agents'` (quoted, from `it.each`'s `$name` interpolation) - `list-state.spec.ts:54`; re-run with the corrected pattern (`--filter "estado de carregamento: 'agents'"`) passes: `list-state.spec.ts:69-70` `expect(maybeEl(fixture,'list-loading')).not.toBeNull(); expect(...MatPaginator...disabled).toBe(true);` | **FAIL - proof-as-written selects 0 tests and exits 0** (the exact `passWithNoTests`-equivalent verify.md warns about); underlying behaviour is proven once the filter is corrected |
| C21 | `500` shows title + retry | `cd src/web && npx ng test --no-watch --include src/app/shared/list-state.spec.ts --filter "estado de erro repete a query: agents"` - **exit 0, 0 tests matched (all 15 skipped)** | Same quoting defect as C20 (`estado de erro repete a query: 'agents'`); corrected filter passes: `list-state.spec.ts:109-115` `expect(text(fixture,'list-error-title')).toBe('Unexpected error'); ...el(fixture,'list-retry').click(); ...expect(calls).toBe(2);` | **FAIL - proof-as-written selects 0 tests and exits 0**; underlying behaviour proven once corrected |
| C22 | Confirm dialog copy + delete/cancel | `cd src/web && npx ng test --no-watch --include src/app/features/ai/agents-list.spec.ts --filter "desativar com confirm\|desativar cancelado"` - exit 0, 2 passed | `agents-list.spec.ts:97` `expect(text(fixture,'active-agent-1')).toBe('Não');` + `:109-110` (zero `DELETE` calls) - `stubConfirm` (`testing.ts:180-182`) replaces `ConfirmService.ask` and ignores its call arguments, so `title:'Desativar agente'`/`confirmLabel:'Desativar'` (AC22, present in source at `agents-list.ts:178-180`) is never asserted by any test | **FAIL - partial**: delete/cancel behaviour proven; dialog copy claim has no evidence anywhere |
| C23 | Create -> `201` -> navigate | `cd src/web && npx ng test --no-watch --include src/app/features/ai/agent-form.spec.ts --filter "201 navega para agents"` - exit 0, 1 passed | `agent-form.spec.ts:60` `expect(TestBed.inject(Router).url).toBe('/ai/agents');` | PASS |
| C24 | `400` shows `errors[field]` outside `mat-error` | `cd src/web && npx ng test --no-watch --include src/app/features/ai/agent-form.spec.ts --filter "400 mostra errors fora de mat-error"` - exit 0, 1 passed | `agent-form.spec.ts:82-83` `expect(text(fixture,'name-error')).toBe('Nome é obrigatório.'); expect(el(fixture,'name-error').closest('mat-error')).toBeNull();` | PASS |
| C25 | No `ai.agent.read` -> forbidden | `cd src/web && npx ng test --no-watch --include src/app/features/ai/agents-list.spec.ts --filter "sem permissao vai para forbidden"` - exit 0, 1 passed | `agents-list.spec.ts:120,124` `expect(router.url).toBe('/forbidden'); expect(text(fixture,'forbidden')).toContain('Sem permissão para esta operação');` | PASS |
| C26 | Flag off hides `AI`/`Agentes` nav | `cd src/web && npx ng test --no-watch --include src/app/shell/shell.spec.ts --filter "esconde AI e Agentes quando a flag esta off"` - exit 0, 1 passed | `shell.spec.ts:85-86` `expect(maybeEl(fixture,'nav-ai')).toBeNull(); expect(maybeEl(fixture,'nav-agents')).toBeNull();` | PASS |
| C27 | Picker sends `agentId`, defaults to seed | `cd src/web && npx ng test --no-watch --include src/app/features/ai/chat.spec.ts --filter "envia agentId do picker"` - exit 0, 1 passed | `chat.spec.ts:108,113` `expect(fixture.componentInstance.agentId()).toBe(SEED_ID); ... expect(received).toEqual({message:'olá',history:[],agentId:SEED_ID});` | PASS |
| C28 | Empty/pending copy | `cd src/web && npx ng test --no-watch --include src/app/features/ai/chat.spec.ts --filter "envia e renderiza o reply\|pendente desativa o envio"` - exit 0, 2 passed | `chat.spec.ts:82` `expect(text(fixture,'chat-empty')).toBe('Faça uma pergunta');` + `:167` `expect(text(fixture,'chat-typing')).toBe('A escrever…');` | PASS |
| C29 | Flat clients, no layer folders | `cd src/web && npx ng test --no-watch --include src/app/architecture.spec.ts --filter "todas as rotas de features.json tem cliente\|sem pastas de camada"` - exit 0, 2 passed | `architecture.spec.ts:52` `expect(missing).toEqual([]);` + `:87` `expect(walk(featuresRoot)).toEqual([]);` | PASS |
| C30 | Real provider POSTs to `BaseUrl` | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~OpenRouterLlmServiceTests.CompleteAsync_ShouldPostChatCompletions_ToBaseUrl\|FullyQualifiedName~MicrosoftAgentFrameworkLlmServiceTests.CompleteAsync_ShouldSendHttp_ToBaseUrl"` - exit 0, 2 passed | `LlmServiceTests.cs:35-36` `Assert.StartsWith("https://openrouter.test/api/v1/chat/completions", handler.RequestUri!.ToString()); Assert.Equal(HttpMethod.Post, handler.Method);` + `:67-69` (MAF host) | PASS |
| C31 | Testing/dev-no-key -> stub | `dotnet test tests/Api.Tests --filter "FullyQualifiedName~LlmServiceRegistrationTests.ShouldRegisterStub_WhenTesting\|FullyQualifiedName~LlmServiceRegistrationTests.ShouldRegisterStub_WhenDevelopmentAndApiKeyEmpty"` - exit 0, 2 passed | `LlmServiceTests.cs:79`, `:86` `Assert.IsType<StubLlmService>(sp.GetRequiredService<ILlmService>());` | PASS - killed by fault F4 |
| C32 | Prod + no key -> fail-fast | `dotnet test tests/Api.Tests --filter FullyQualifiedName~LlmServiceRegistrationTests.ShouldFailFast_WhenProductionEnableAiAndApiKeyEmpty` - exit 0, 1 passed | `LlmServiceTests.cs:107-108` `var ex = await Assert.ThrowsAsync<InvalidOperationException>(() => host.StartAsync()); Assert.Contains("Ai:Llm:ApiKey", ex.Message, ...);` - real `HostBuilder.StartAsync()`, crosses the host boundary | PASS |
| C33 | Usage records real provider/model | `dotnet test tests/Api.Tests --filter FullyQualifiedName~ChatAiHandlerTests.Handle_ShouldTrackConfiguredProviderAndModel` - exit 0, 1 passed | `ChatAiHandlerTests.cs:175-176` `Assert.Equal(LlmProviders.OpenRouter, record.Provider); Assert.Equal("openai/gpt-4o-mini", record.Model);` | PASS |
| C34 | LLM HTTP failure -> `500` | `dotnet test tests/Api.Tests --filter FullyQualifiedName~ChatAiTests.ChatAi_ShouldReturn500_WhenLlmHttpFails` - exit 0, 1 passed | `ChatAiTests.cs:89-91` `Assert.Equal(HttpStatusCode.InternalServerError, response.StatusCode); Assert.Equal("Unexpected error", problem?.Title);` | PASS |
| C35 | Create file -> `201` w/ `fileId`,`name` | `dotnet test tests/Api.Tests --filter FullyQualifiedName~CreateAgentFileTests.Handle_ShouldCreateFile_WhenInputIsValid` - exit 0, 1 passed | `CreateAgentFileTests.cs:31-33` - handler-level only; no HTTP test exists for a successful file `POST` | **FAIL - level gap** (AC35 text: "...responder `201`...") |
| C36 | List only that agent's files | `dotnet test tests/Api.Tests --filter FullyQualifiedName~ListAgentFilesTests.Handle_ShouldListOnlyThatAgentsFiles` - exit 0, 1 passed | `CreateAgentFileTests.cs:104-105` `Assert.Single(listed); Assert.Equal("a.txt", listed[0].Name);` | PASS (no status code named); Coverage note: `200` for this route unproven at HTTP |
| C37 | Cross-tenant `agentId` on files -> `404` | `dotnet test tests/Api.Tests --filter FullyQualifiedName~CreateAgentFileTests.Handle_ShouldThrow_WhenAgentIsInAnotherTenant` - exit 0, 1 passed | `CreateAgentFileTests.cs:48-51` `Assert.ThrowsAsync<NotFoundException>` - handler-level only | **FAIL - level gap** (AC37 text: "...SHALL responder `404`...") |
| C38 | Delete file -> later `GET` `404` | `dotnet test tests/Api.Tests --filter FullyQualifiedName~DeleteAgentFileTests.Handle_ShouldRemoveFile_WhenFileExists` - exit 0, 1 passed | `CreateAgentFileTests.cs:132-133` (`DeleteAgentFileTests`) `Assert.ThrowsAsync<NotFoundException>` via `get.Handle(...)` - handler-level only | **FAIL - level gap** (AC38 text: "...SHALL responder `404`") |
| C39 | `read_agent_file` returns content, scoped to same agent | `dotnet test tests/Api.Tests --filter FullyQualifiedName~AgentFileToolTests.ReadAgentFile_ShouldReturnContent_ForSameAgent` - exit 0, 1 passed | `CreateAgentFileTests.cs:170` `Assert.Contains("secret-body", json);` - proves content-return; the test never attempts a cross-agent read, so "só desse agente" is unasserted | **FAIL - partial**: content-return proven; scoping half confirmed **unproven** by fault F5 (surviving mutant) |
| C40 | No vector/MCP/embedding surface | `dotnet test tests/ArchitectureTests --filter FullyQualifiedName~SliceStructureTests.Features_ShouldNotContain_LayerFolders` - exit 0, 1 passed; `dotnet test tests/Api.Tests --filter FullyQualifiedName~AgentFileToolTests.Module_ShouldNotExposeVectorOrMcpEndpoints` - exit 0, 1 passed | `CreateAgentFileTests.cs:178-180` `Assert.DoesNotContain("vector", features, ...); Assert.DoesNotContain("mcp", ...); Assert.DoesNotContain("embedding", ...);` | PASS |
| C41 | Unauthenticated `GET /agents` -> `401` | `dotnet test tests/Api.Tests --filter FullyQualifiedName~CreateAgentTests.Get_ShouldReturn401_WhenNotAuthenticated` - exit 0, 1 passed | `CreateAgentTests.cs:182` `Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);` | PASS |
| C42 | `agents-list` arrangement | `cd src/web && npx ng test --no-watch --include src/app/features/ai/agents-list.spec.ts --filter "arranjo igual a tenants"` - exit 0, 1 passed | `agents-list.spec.ts:81-83` DOM-order assertions (`compareDocumentPosition`) header→search→list-state→table→paginator | PASS |
| C43 | Chat picker above history, full width | `cd src/web && npx ng test --no-watch --include src/app/features/ai/chat.spec.ts --filter "picker acima do historico"` - exit 0, 1 passed | `chat.spec.ts:139-141` `expect(picker!.compareDocumentPosition(history) & Node.DOCUMENT_POSITION_FOLLOWING).toBeTruthy(); expect(getComputedStyle(picker!).width).not.toBe('0px');` | PASS |
| C44 | Unauthenticated file `GET` -> `401` | `dotnet test tests/Api.Tests --filter FullyQualifiedName~CreateAgentFileTests.Get_ShouldReturn401_WhenNotAuthenticated` - exit 0, 1 passed | `CreateAgentFileTests.cs:66` `Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode);` | PASS |

31 PASS, 13 FAIL (C1, C4, C5, C6, C11, C14, C20, C21, C22, C35, C37, C38, C39).

### Flaky proof (separate from the level gaps above)

`UpdateAgentTests.Handle_ShouldThrow_WhenAgentDoesNotExist` (one of C6's three cited proofs) intermittently fails: 3 of 5 consecutive runs of `dotnet test tests/Api.Tests --filter "FullyQualifiedName~Api.Tests.Ai"` failed with `System.ArgumentException: An item with the same key has already been added. Key: 00000000-0000-0000-0000-000000000001` inside `TestServiceFactory.SeedTenants`. Root cause: `GetAgentTests`, `UpdateAgentTests`, and `DeactivateAgentTests` each have a method literally named `Handle_ShouldThrow_WhenAgentDoesNotExist` and each calls `TestServiceFactory.CreateWithAi(nameof(Handle_ShouldThrow_WhenAgentDoesNotExist))` (`CreateAgentTests.cs:293,400,456`) - three different xUnit test classes (run in parallel by default, one collection per class) racing to seed the *same* EF Core InMemory database name. This exact pattern pre-exists in `tests/Api.Tests/Tenants/{CreateTenantTests.cs:131, TenantManagementTests.cs:46,83}` (`nameof(Handle_ShouldThrow_WhenTenantDoesNotExist)`), so it is an inherited repo convention, not something this feature introduced - but it does make one of C6's three proofs unreliable.

## Coverage

| Set (size) | Recomputed from | Member -> proof | Unproven |
| --- | --- | --- | --- |
| `POST /api/v1/ai/agents` statuses (6) | plan Surface table | `400` C7 · `401` C41 · `403` C9 · `404` C8 · `409` C3 | `201` - only handler-level (C1) |
| `GET /api/v1/ai/agents` statuses (4) | plan Surface table | `401` C41 · `403` C9 · `404` C8 | `200` - only handler-level (C2) |
| `GET /api/v1/ai/agents/{agentId}` statuses (4) | plan Surface table | `401` C41(cross-route) · `403` C9 | `200` (no proof, handler- or HTTP-level, ever calls `GetAgentHandler`'s success path through an assertion of its own) · `404` - only handler-level (C6) |
| `PUT /api/v1/ai/agents/{agentId}` statuses (6) | plan Surface table | `400` C7 · `403` C9 · `409` C3 | `200` - only handler-level (C4) · `404` - only handler-level (C6) |
| `DELETE /api/v1/ai/agents/{agentId}` statuses (5) | plan Surface table | `403` C9 | `204` - only handler-level (C5) · `404` - only handler-level (C6) · `409` - only handler-level (C11) |
| `POST /api/v1/ai/chat` statuses (4) | plan Surface table | `200` C13 · `401` C16 · `500` C34 | `404` - only handler-level for the unknown/inactive-agent branch (C14); the `EnableAI=false` `404` branch (C17) *is* HTTP-proven |
| `POST /api/v1/ai/agents/{agentId}/files` statuses (5) | plan Surface table | `401` C44 · `403` C9 | `201` - only handler-level (C35) · `400` (no proof at any level for this route's validator) · `404` - only handler-level (C37) |
| `GET /api/v1/ai/agents/{agentId}/files` statuses (4) | plan Surface table | `401` C44 · `403` C9 | `200` - only handler-level (C36) · `404` (no test targets this route's own 404 path; C37's proof is on `CreateAgentFile`) |
| `GET …/files/{fileId}` statuses (4) | plan Surface table | `401` C44 · `403` C9 | `200` - **no proof of any kind** (`GetAgentFileHandler`/`GetAgentFileQuery` is never exercised on its success path by any test; C39 exercises `ReadAgentFileTool` directly, a different code path, not this handler/endpoint) · `404` - only handler-level (C38, via `DeleteAgentFileTests`) |
| `DELETE …/files/{fileId}` statuses (4) | plan Surface table | `401` C44 · `403` C9 | `204` (no proof of any kind for the delete's own success status) · `404` - only handler-level (C38) |
| LLM providers (2) | plan Assumptions row | OpenRouter C30 · MicrosoftAgentFramework C30 | - |
| LLM registration (3) | plan Q4 section | Testing stub C31 · Development empty-key stub C31 · Production fail-fast C32 | - |
| one-way doors (7) | plan Landing table | Agent aggregate C1(persistence proven; status not) · mutable Update C4(mutation proven; status not) · unique name C3 · AgentFile C35(persistence proven; status not) · LLM switch C30 · chat agentId C12,C13,C14(404 branch not HTTP-proven) · RBAC C9,C16 | - |
| Relations entities (2) | plan `Relations` mermaid | Agent C1 · AgentFile C35 | - |
| screens (3) | plan Observable table | agents-list C19,C20(cmd defect),C21(cmd defect),C22(partial),C25,C42 · agent-form C23,C24 · chat C27,C28,C43 | - |
| agents-list copy (2) | plan S3 ACs | empty `Nenhum agente` C19 | confirm dialog copy (`Desativar agente`/`Desativar`) - no assertion anywhere (C22) |
| chat copy (3) | plan S3 ACs | `Faça uma pergunta` C28 · `A escrever…` C28 · picker label `Agente` C27 | - |
| file-tool scoping (1) | AC39 text ("só desse agente") | - | cross-agent `read_agent_file` rejection - confirmed unproven by fault F5 (surviving mutant) |

## Test policy rows

| Row | Files it classifies | Required proof | Expectation met |
| --- | --- | --- | --- |
| Decide, crossed by a boundary - unique name (`CreateAgentHandler`) | `CreateAgent.cs` | boundary + own level | yes (C3) |
| Decide, crossed by a boundary - last-active guard (`DeactivateAgentHandler`) | `DeactivateAgent.cs` | boundary + own level | **no** - own level only (C11); checks.md's own Evidence row admits this ("409 at HTTP (C3)" excludes C11) |
| Decide, crossed by a boundary - not-found guards (`GetAgentHandler`/`UpdateAgentHandler`/`DeactivateAgentHandler`/`CreateAgentFileHandler`/`DeleteAgentFileHandler`) | `GetAgent.cs`, `UpdateAgent.cs`, `DeactivateAgent.cs`, `CreateAgentFile.cs`, `DeleteAgentFile.cs` | boundary + own level | **no** - own level only for every one of them (C6, C37, C38) |
| `LlmServiceResolver` (Testing/Dev/Prod × key × provider) | `LlmServiceResolver.cs` | registration + HTTP-client boundary | yes (C30, C31, C32) |
| `OpenRouterLlmService`/`MicrosoftAgentFrameworkLlmService` map+POST | `OpenRouterLlmService.cs`, `MicrosoftAgentFrameworkLlmService.cs` | fake-handler boundary | yes (C30) |
| `agents-list.ts`/`agent-form.ts`/`chat.ts` screen state, Vitest+MSW | `agents-list.ts`, `agent-form.ts`, `chat.ts` | one Vitest+MSW case per decision | **partially** - C20/C21's cited commands run 0 tests as written (procedural, see above); C22's confirm-dialog copy is asserted nowhere because `stubConfirm` erases the call arguments |
| Endpoint `Map*` methods, instrumentation, "covered by HTTP tests" | all 10 `*Endpoint` classes in `Features/Ai` | none of its own - covered by consumers | **no** - this is the row that fails hardest. Of the module's 10 HTTP endpoints, only `ChatAi`'s success path (`200`, C13) and the shared error paths (`400`/`401`/`403`/`404`-flag/`409`-create, C3/C7/C8/C9/C41/C44) are ever exercised through `TestWebApplicationFactory`. The success-path `Results.Created(...)`/`Results.Ok(...)`/`Results.NoContent()` mapping, route-parameter binding, and JSON shape of `AgentOutput`/`AgentFileOutput` for `CreateAgent`, `ListAgents`, `GetAgent`, `UpdateAgent`, `DeactivateAgent`, `CreateAgentFile`, `ListAgentFiles`, `GetAgentFile`, `DeleteAgentFile` are **never** asserted through an actual HTTP call anywhere in the diff - only through direct handler invocation, which bypasses the endpoint entirely |

Cost note from checks.md ("Without those rows a stub named Azure would still pass ChatAiHandlerTests") is honored for the LLM-provider rows; it is not honored for the CRUD endpoint-mapping row above - a regression that swapped `Results.Created` for `Results.Ok` on `CreateAgent`, or dropped a field from `AgentOutput`'s JSON contract, would pass every cited proof in `checks.md`.

## Faults injected

Isolated via `git worktree add <scratch> HEAD` (never `git stash`); baseline `git status --porcelain` recorded before, worktree discarded and porcelain re-diffed after (identical, see header note).

| Mutation | Location | Killed |
| --- | --- | --- |
| Removed the unique-name conflict guard | `CreateAgent.cs:35-37` (`Handle`) | yes - `Handle_ShouldThrow_WhenNameAlreadyExists` and `Post_ShouldReturn409_WhenNameIsDuplicate` both fail (`Assert.Throws` gets no exception; HTTP returns `201` instead of `409`) |
| `active <= 1` -> `active <= 0` (off-by-one) in the last-active guard | `DeactivateAgent.cs:29` (`Handle`) | yes - `Handle_ShouldThrow_WhenLastActiveAgent` fails (no exception thrown) |
| Passed `null` instead of `agent.ToolNames` as the allowlist | `ChatAi.cs:50` (`Handle`, call into `agentLoop.RunAsync`) | yes - `Handle_ShouldUseAgentAllowlist_WhenAgentIdIsProvided` fails (`llm.Last.Tools` lists all 4 registered tools instead of `["get_tenant_info"]`) |
| Inverted the `Testing`-environment check | `LlmServiceResolver.cs:10` (`UseStub`) | yes - `ShouldRegisterStub_WhenTesting` fails (`ILlmService` resolves to `OpenRouterLlmService` instead of `StubLlmService`) |
| Dropped the per-agent scoping guard (`file.AgentId != agentId`) | `AgentFileTools.cs` (`ReadAgentFileTool.ExecuteAsync`) | **no** - `ReadAgentFile_ShouldReturnContent_ForSameAgent` still passes; confirms the Coverage gap on AC39's "só desse agente" clause is real, not theoretical |

4 of 5 faults killed; 1 surviving mutant (file-scoping), which is a finding, not a footnote.

## Gate

```
$ python3 .cursor/skills/tlc-spec-lean/scripts/validate_verification.py agentes --root /Users/luissoares/Repos/Feature.template
  ERROR agentes: verdict is FAIL - route the ranked gaps back as fixes, then re-verify

validate_verification: 1 error(s), 0 warning(s) across [agentes]
```
Exit code 1. This is the expected gate response to a legitimate FAIL verdict (the script short-circuits to this single error once the verdict line reads FAIL, per its own source at `validate_verification.py:164-170,192-193`) - not a defect in this report's structure.

## Ranked gaps

1. **Endpoint success paths never cross HTTP** - of the module's 10 endpoints, only `ChatAi`'s `200` and shared error paths (`400/401/403/404-flag/409`) are exercised through `TestWebApplicationFactory`. `CreateAgent`'s `201`, `ListAgents`'/`GetAgent`'s `200`, `UpdateAgent`'s `200`, `DeactivateAgent`'s `204`, `CreateAgentFile`'s `201`, `ListAgentFiles`'/`GetAgentFile`'s `200`, `DeleteAgentFile`'s `204` are proven only by calling the handler directly, bypassing `Results.Created/Ok/NoContent` and the JSON contract entirely. Checks C1, C4, C5, C35 - `CreateAgent.cs`, `UpdateAgent.cs`, `DeactivateAgent.cs`, `CreateAgentFile.cs` (endpoint `Map` methods).
2. **Not-found guards never cross HTTP** - `GetAgentHandler`, `UpdateAgentHandler`, `DeactivateAgentHandler`, `CreateAgentFileHandler`, `DeleteAgentFileHandler` all throw `NotFoundException` correctly, but no test calls the actual `GET`/`PUT`/`DELETE` route with an unknown id and checks for `404`. Checks C6, C37, C38 - `GetAgent.cs:13-14`, `UpdateAgent.cs:33-34`, `DeactivateAgent.cs:23-24`, `CreateAgentFile.cs:33-34`, `DeleteAgentFile.cs:25-26,28-30`.
3. **Last-active guard's `409` never crosses HTTP** - `DeactivateAgentHandler`'s last-active guard is proven only at handler level; checks.md's own Test-policy Evidence section admits this. Check C11 - `DeactivateAgent.cs:26-31`.
4. **`GetAgentFileHandler` success path has zero coverage of any kind** - no test, handler- or HTTP-level, ever calls `GetAgentFileQuery`/`GetAgentFileEndpoint` on a file that exists; the only exercise of file-reading is `AgentFileToolTests` calling `ReadAgentFileTool` directly, a different code path. `GetAgentFile.cs:8-23`.
5. **Chat's unknown/inactive-agent `404` never crosses HTTP** - `ChatAiHandlerTests` proves the `NotFoundException` at handler level; no `ChatAiTests` HTTP case sends an unknown/inactive `agentId`. Check C14 - `ChatAi.cs:83-96`.
6. **`C20`/`C21` proof commands select 0 tests and exit 0** - `--filter "estado de carregamento: agents"` / `"estado de erro repete a query: agents"` don't match the actual (quoted) `it.each` test names in `list-state.spec.ts:54,93`; both silently "pass" with nothing run. The underlying behaviour is fine once the filter is corrected. `checks.md` proof lines for C20/C21.
7. **Confirm-dialog copy (AC22) is asserted nowhere** - `stubConfirm` (`src/web/testing.ts:180-182`) replaces `ConfirmService.ask` and discards its arguments, so `title:'Desativar agente'`/`confirmLabel:'Desativar'` (`agents-list.ts:178-180`) has no test evidence. Check C22.
8. **File-scoping on `read_agent_file` is a confirmed surviving mutant** - removing `file.AgentId != agentId` in `ReadAgentFileTool.ExecuteAsync` (`AgentFileTools.cs`) left `AgentFileToolTests.ReadAgentFile_ShouldReturnContent_ForSameAgent` green. AC39's "só desse agente" clause is unproven. Check C39.
9. **Flaky proof for C6** - `UpdateAgentTests.Handle_ShouldThrow_WhenAgentDoesNotExist` fails ~3/5 runs from an EF InMemory database-name collision across three test classes sharing the literal method name `Handle_ShouldThrow_WhenAgentDoesNotExist` (`CreateAgentTests.cs:293,400,456`); pre-existing repo convention (also in `tests/Api.Tests/Tenants/*`), not introduced by this feature, but it makes one of C6's proofs non-deterministic.
