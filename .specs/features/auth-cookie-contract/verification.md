# Auth cookie contract verification

**Verdict**: FAIL
**Profile**: ui
**Diff range**: 902d206..working tree (uncommitted)
**Round**: 1 - full
**Verifier**: independent sub-agent (author != verifier)

Three of eighteen checks are not proven. C17 and C18 cite a test class, `SolutionFileTests`, that
does not exist anywhere in the repository - the `--filter` matches nothing and VSTest exits `0`, so
both checks were green over no test at all. C6 claims a `204` and its only proof asserts a handler
return value below the HTTP boundary. The shipped behaviour is otherwise sound: the refresh token
never reaches script, a revoked cookie stops working, and the contract guard fires in both
directions - all five injected mutants were killed.

## Binding sources

| Source | Opened | Contradiction | Uncovered |
| --- | --- | --- | --- |
| plan `Sources` - "conversa" | no - not a retrievable artifact; nothing exists to open | - | - |
| `.specs/features/web-frontend/plan.md` - door 3 (sessão no browser), door 5 (cliente à mão) | yes - `Landing` and `Observable` read in full | none - door 3 is explicitly superseded by this plan's `Landing`, and the code follows the superseding shape (`src/web/src/app/core/session/session.store.ts:19-22` `StoredAuth { tenantKey, user }`) | `screen shell` / "ação destrutiva confirma" gains a new branch - confirm cancelled ⇒ no `POST /identity/logout` - and no check covers it; `src/web/src/app/shell/shell.spec.ts:36` only ever runs `stubConfirm(true)` |
| `src/Api/Features/Identity/Login.cs` | yes - `LoginEndpoint:98` `RefreshCookie.Write(context, result.RefreshToken)`, `:100-104` returns `AuthTokenResponse` with no token | none | - |
| `src/Api/Features/Identity/RefreshAccessToken.cs` | yes - `:109-110` cookie is the only accepted source, `:113` rewrites it after rotation | none | - |
| `.specs/features/auth-cookie-contract/plan.md` (`Surface`, `Relations`, `Landing`, `Observable`) | yes | none between the checks and `Landing`; the shipped shape matches the **superseding** door, not the superseded one (see below) | `document docs/security/RBAC_MATRIX.md` is an `Observable` row landed on AC 4 and no check covers it - the doc was updated (`docs/security/RBAC_MATRIX.md:31`) but nothing would catch its removal |
| `src/Api/openapi.json` - the contract this feature makes authoritative | yes - 30 operations, all `/api/v1/**`; login `200,400,401,409`, refresh `200,401,404,409`, logout `204` | `Surface` names `429` for all three routes; the document declares `429` on none of them, and the drift guards compare paths and methods only, so a status drift is invisible | - |

**Superseded vs superseding door.** The `Landing` row "documento OpenAPI gerado no build" is explicitly
superseded by "documento versionado, regenerado pelo host de testes". What shipped is the superseding
row: `src/Api/Host/Configurations/OpenApiHostConfiguration.cs:11` maps the document under Development
**and** Testing while `:15-21` keeps Scalar development-only; `src/Api/openapi.json` is a versioned file
regenerated by `UPDATE_OPENAPI=1` (`tests/E2ETests/Common/OpenApiDocumentTests.cs:48-52`); and
`src/Api/Api.csproj` carries no `Microsoft.Extensions.ApiDescription.Server` reference, so nothing
generates the document at build time. Correct - but the test cited by C13 is named
`Document_ShouldBeGenerated_AtBuildTime` (`tests/ArchitectureTests/OpenApiContractTests.cs:89`) after
the rejected shape, and it asserts route counts, not build-time generation.

**Screen enumeration (`ui`).** This feature adds no screen, no control, no indicator and no region.
The only selector-reachable change is on `screen shell`: `Sair` (`src/web/src/app/shell/shell.ts:61`,
label unchanged) now awaits `POST /api/v1/identity/logout` before `session.clear()`
(`shell.ts:100-106`). The toolbar's composition - nav links in fixed order, spacer, tenant, user,
`Sair` - is untouched, and `screen login` renders exactly what `web-frontend` fixed. The two branches
of the new interaction are: confirmed ⇒ POST then clear (C11), cancelled ⇒ nothing (no check; listed
`Uncovered` above, and the repo's own convention covers the analogous case at `web-frontend` C24).

## Checks

| Check | Claim | Proof run | Evidence | Result |
| --- | --- | --- | --- | --- |
| C1 | login emits `Set-Cookie pt_refresh` with `httponly`, `samesite=strict`, `path=/api/v1/identity` | `dotnet test tests/E2ETests --filter "…Login_ShouldSetHttpOnlyRefreshCookie"` - Aprovado, 1 test | `tests/E2ETests/Identity/IdentityAuthE2ETests.cs:72-76` - `Assert.Contains("pt_refresh=", cookie)` · `Assert.Contains("httponly", cookie, OrdinalIgnoreCase)` · `Assert.Contains("samesite=strict", …)` · `Assert.Contains("path=/api/v1/identity", …)` · `Assert.Contains("max-age=", …)` | PASS |
| C2 | login body carries no `refreshToken` | same batch - Aprovado | `IdentityAuthE2ETests.cs:89-90` - `Assert.False(body.RootElement.TryGetProperty("refreshToken", out _))` · `Assert.True(body.RootElement.TryGetProperty("accessToken", out _))` | PASS |
| C3 | refresh reads the cookie and returns a different `pt_refresh` | same batch - Aprovado | `IdentityAuthE2ETests.cs:104,106` - `Assert.Equal(HttpStatusCode.OK, refresh.StatusCode)` · `Assert.NotEqual(firstCookie, secondCookie)` | PASS |
| C4 | refresh without the cookie returns `401` | same batch - Aprovado | `IdentityAuthE2ETests.cs:117` - `Assert.Equal(HttpStatusCode.Unauthorized, response.StatusCode)` | PASS |
| C5 | logout with cookie returns `204`, clears the cookie, and the old token then fails `401` | same batch - Aprovado | `IdentityAuthE2ETests.cs:131` - `Assert.Equal(HttpStatusCode.NoContent, logout.StatusCode)`; `:132-134` - `Assert.Contains(logout.Headers.GetValues("Set-Cookie"), header => header.StartsWith("pt_refresh=;", StringComparison.Ordinal))`; `:138` - `Assert.Equal(HttpStatusCode.Unauthorized, refresh.StatusCode)` | PASS |
| C6 | logout without cookie returns `204` and changes no record | `dotnet test tests/Api.Tests --filter "…LogoutTests.Handle_ShouldNoOp_WhenTokenIsMissing"` - Aprovado | `tests/Api.Tests/Identity/LogoutTests.cs:24` - `Assert.False(revoked)` - a handler return value. **Level gap**: the claim names `204`, and no proof anywhere issues a cookieless `POST /api/v1/identity/logout` or observes its status. The "changes no record" half is likewise unasserted - it is inferred from `Logout.cs:29-30`, not measured | FAIL |
| C7 | the cookie carries `Secure` over HTTPS and not over plain HTTP | `dotnet test tests/Api.Tests --filter "…RefreshCookieTests.Options_ShouldSetSecure_ByScheme"` - Aprovado ×2 (`isHttps: True`, `isHttps: False`) | `tests/Api.Tests/Identity/LogoutTests.cs:62` - `Assert.Equal(isHttps, options.Secure)`, `[InlineData(true)]`/`[InlineData(false)]` at `:53-54`. Targets the check-defined value, but one layer below the wire - see the level gap in `Test policy rows` | PASS |
| C8 | every front request to `/api/v1/**` carries `withCredentials` | proof as written is unrunnable (see findings); ran `npx ng test --no-watch --reporters=verbose --include src/app/core/http/api.interceptor.spec.ts` - `✓ envia withCredentials` | `src/web/src/app/core/http/api.interceptor.spec.ts:60` - `expect(decorated.withCredentials).toBe(true)` | PASS |
| C9 | after login `localStorage['pt.auth']` holds `tenantKey` and `user` and no token field | same runner, `--include src/app/features/identity/login.spec.ts` - `✓ guarda a sessao e navega para users` | `src/web/src/app/features/identity/login.spec.ts:40` - `expect(Object.keys(stored).sort()).toEqual(['tenantKey', 'user'])`; `:43` - `expect(raw.toLowerCase()).not.toContain('token')` | PASS |
| C10 | the front's refresh is emitted with no token body and the session survives | same runner - `✓ 401 renova e repete` | `src/web/src/app/core/http/api.interceptor.spec.ts:92-94` - `expect(body.totalCount).toBe(1)` · `expect(requests.filter(r => r.url.pathname === REFRESH)).toHaveLength(1)` · `expect(…USERS)).toHaveLength(2)`. Precision gap: "sem corpo com token" is not asserted - it lives at `src/web/src/app/core/http/refresh-coordinator.ts:29` (`post(…, {})`) | PASS |
| C11 | `Sair` calls `POST /api/v1/identity/logout` before clearing local state | same runner, `--include src/app/shell/shell.spec.ts` - `✓ logout chama a API` | `src/web/src/app/shell/shell.spec.ts:48-50` - `expect(requests.filter(r => r.url.pathname === '/api/v1/identity/logout')).toHaveLength(1)`; `:51` - `expect(localStorage.getItem(AUTH_STORAGE_KEY)).toBeNull()`. Precision gap: the ordering the claim names is unasserted - clearing first would still pass | PASS |
| C12 | against the real API, reloading renews the session with no token in `localStorage` | `PW_CHANNEL=chrome npx playwright test e2e/auth.spec.ts -g "renova o token expirado"` - 1 passed (real API on :5080, `ng serve` bundle) | `src/web/e2e/auth.spec.ts:20` - `expect(refreshCalls.length).toBeGreaterThan(0)`; `:30` - `expect(storage.local.toLowerCase()).not.toContain('token')`; `:32` - `expect(storage.cookies).not.toContain('pt_refresh')` | PASS |
| C13 | the served document equals the committed `src/Api/openapi.json` and covers the 30 `features.json` routes | `dotnet test tests/E2ETests --filter "…Document_ShouldMatch_TheCommittedContract"` - Aprovado; `dotnet test tests/ArchitectureTests --filter "…Document_ShouldBeGenerated_AtBuildTime"` - Aprovado | `tests/E2ETests/Common/OpenApiDocumentTests.cs:60-62` - `Assert.True(committed == generated, "src/Api/openapi.json is out of date. …")`; `tests/ArchitectureTests/OpenApiContractTests.cs:93-94` - `Assert.Equal(FeatureRoutes().Count, routes.Count)` · `Assert.Contains("POST /api/v1/identity/login", routes)`. Second proof is misnamed after the superseded door and asserts a count, not build-time generation | PASS |
| C14 | a `features.json` route absent from `openapi.json` fails the architecture tests | `dotnet test tests/ArchitectureTests --filter "…EveryFeatureRoute_ShouldExist_InTheDocument"` - Aprovado | `tests/ArchitectureTests/OpenApiContractTests.cs:73-75` - `Assert.True(missing.Count == 0, "Routes in features.json missing from openapi.json: " + string.Join(", ", missing))` over `FeatureRoutes().Except(DocumentedRoutes())` | PASS |
| C15 | an `/api/v1/**` route of the document absent from `features.json` fails the architecture tests | `dotnet test tests/ArchitectureTests --filter "…EveryDocumentedRoute_ShouldExist_InFeaturesJson"` - Aprovado | `tests/ArchitectureTests/OpenApiContractTests.cs:83-85` - `Assert.True(missing.Count == 0, "Routes in openapi.json missing from features.json: " …)` over `DocumentedRoutes().Except(FeatureRoutes())` | PASS |
| C16 | a path called by a front client and absent from `openapi.json` fails the front test run | proof as written unrunnable; ran `npx ng test --no-watch --include src/app/architecture.spec.ts` - `✓ clientes so chamam rotas documentadas` | `src/web/src/app/architecture.spec.ts:65` - `expect(called.filter((path) => !documented.has(path))).toEqual([])`; `:64` - `expect(called.length).toBeGreaterThan(0)` guards the vacuous pass | PASS |
| C17 | `dotnet build` at the root compiles the four projects and exits zero | `dotnet test tests/ArchitectureTests --filter "FullyQualifiedName~SolutionFileTests.Solution_ShouldListEveryProject"` - **"Nenhum teste corresponde ao filtro"**, exit `0` | **no evidence**. `rg`/`grep -rn "SolutionFileTests\|Solution_Should"` over the whole tree (excluding `node_modules`, `.git`, `bin`, `obj`) hits only `checks.md:68` and `checks.md:71`. The class does not exist. The claim is separately true in fact - `dotnet build Product.Template.sln` is green over `Api`, `Api.Tests`, `ArchitectureTests`, `E2ETests` - but nothing in the repo proves it, and the cited proof would be a static file test for a claim about a build exit code even if it existed | FAIL |
| C18 | the solution's project GUIDs are the ones `.template.config/template.json` regenerates | `dotnet test tests/ArchitectureTests --filter "FullyQualifiedName~SolutionFileTests.ProjectGuids_ShouldMatch_TemplateConfig"` - **matches nothing**, exit `0` | **no evidence**. Same search; the class does not exist. The four GUIDs in `Product.Template.sln:5,7,9,11` do match `.template.config/template.json` `guids`, verified by hand, but no test asserts it | FAIL |

## Coverage

Recomputed from the authority over each set, not read back from the author's table.

| Set (size) | Recomputed from | Member -> proof | Unproven |
| --- | --- | --- | --- |
| `POST /api/v1/identity/login` statuses (5) | `src/Api/openapi.json` `paths./api/v1/identity/login.post.responses` = `200,400,401,409`, plus `src/Api/Features/Identity/Login.cs:109` `RequireRateLimiting(auth)` ⇒ `429` | 200 C1/C2 (`IdentityAuthE2ETests.cs:71,90`) · 400 web-frontend C3 · 401 web-frontend C2 · 409 web-frontend C15 · 429 web-frontend C13 | - |
| `POST /api/v1/identity/refresh` statuses (5) | `openapi.json` = `200,401,404,409`, plus `RefreshAccessToken.cs:124` `RequireRateLimiting(auth)` ⇒ `429` | 200 C3 (`:104`) · 401 C4 (`:117`) · 404 web-frontend C14 · 409 web-frontend C15 · 429 web-frontend C13 | - |
| `POST /api/v1/identity/logout` statuses (2 documented, 3 reachable) | `openapi.json` = `204`; `src/Api/Features/Identity/Logout.cs:48` `Results.NoContent()` on both branches, `:55` `RequireRateLimiting(auth)` | `204` with cookie C5 (`:131`) · `204` without cookie **no proof** · `429` **no proof** | `204` on the cookieless branch (C6 stops at `Assert.False(revoked)`); `429` - the row credits `web-frontend` C13, which stubs a `429` on `/login` in MSW and says nothing about `/logout` or about the API emitting one |
| cookie attributes (5) | `src/Api/Features/Identity/RefreshCookie.cs:19-26` and the wire - live `POST /login` on `:5080` returned `Set-Cookie: pt_refresh=…; max-age=2592000; path=/api/v1/identity; samesite=strict; httponly` | `HttpOnly` C1 (`:73`) · `SameSite=Strict` C1 (`:74`) · `Path` C1 (`:75`) · `Max-Age` C1 (`:76`, presence only) · `Secure` C7 (`LogoutTests.cs:62`, `CookieOptions`, not the header) | `Secure` at the HTTP boundary - no test observes the attribute on a real `Set-Cookie`, present or absent; `Max-Age` value - `Landing` fixes `dias*86400` (2592000 for the 30-day default at `src/Api/appsettings.json:19`) and only the prefix is asserted |
| one-way doors in the plan's `Landing` (6 rows, 5 live) | `.specs/features/auth-cookie-contract/plan.md` `Landing` | refresh em cookie C1 · corpo sem token C2 (`:89`) · rota `logout` C5 (`:131`) · documento **versionado** C13 (`OpenApiDocumentTests.cs:60`) + `OpenApiHostConfiguration.cs:11` · documento gerado no build - superseded, correctly absent (`src/Api/Api.csproj` has no `Microsoft.Extensions.ApiDescription.Server`) · solução na raiz **no proof** | `ficheiro de solução na raiz` - C17 and C18 cite tests that do not exist; the only evidence is my own `dotnet build Product.Template.sln` run |
| `features.json` ↔ `openapi.json` routes (30) | `features.json` - 30 slices, 30 distinct `route` values; `src/Api/openapi.json` - 30 operations, all under `/api/v1/` | all 30 in both directions - C14 (`OpenApiContractTests.cs:73`) and C15 (`:83`) are set differences, not samples | - |
| contract drift directions (2) | `tests/ArchitectureTests/OpenApiContractTests.cs` | features.json → documento C14 (`:73`) · documento → features.json C15 (`:83`) | - |
| front surfaces touched (3) | the files this change edits under `src/web/src/app` | interceptor C8 (`api.interceptor.spec.ts:60`) · sessão C9 (`login.spec.ts:40`) · shell C11 (`shell.spec.ts:49`) | - |
| bootstrap: HTTP providers (3 assemblies) | each assembly file opened directly | `src/web/src/app/app.config.ts:10` - `provideHttpClient(withInterceptors([apiInterceptor])),` · `src/web/src/test-providers.ts:20` - `provideHttpClient(withInterceptors([apiInterceptor])),` · Playwright assembly = the real app, `src/web/playwright.config.ts:21` `command: 'npm run start'`, exercised live by C12. C8 runs only inside the TestBed assembly; `app.config.ts` is reached by `src/web/src/app/architecture.spec.ts:107` (textual) and by C12 against the served bundle | - |

## Test policy rows

| Row | Files it classifies | Required proof | Expectation met |
| --- | --- | --- | --- |
| Decide, atravessado por uma fronteira | `src/Api/Features/Identity/RefreshCookie.cs` | boundary C1 · own level C7 | no - the own-level side is met (`LogoutTests.cs:62`, `[InlineData(true)]`/`[InlineData(false)]` covers both rows of the `Secure` decision table), but the boundary side is not: C1 runs over HTTP (`IdentityAuthE2ETests.cs:71-76`) and asserts neither the presence nor the absence of `secure`. The row demands "o contrato na fronteira" for the decision it classifies |
| Decide, atravessado por uma fronteira | `src/Api/Features/Identity/Logout.cs` | boundary C5 · own level C6 | no - own level is met for both branches (`LogoutTests.cs:24` `Assert.False(revoked)`, `:42` `Assert.True(revoked)` plus `:45-46` `Assert.ThrowsAsync<UnauthorizedAccessException>`), and the revoke branch is met at the boundary (`IdentityAuthE2ETests.cs:131,138`); the no-cookie branch never crosses the boundary, so the `204` it claims is unproven |
| Decide, não atravessado por uma fronteira | none - the `Evidence` block classifies no file here | - | n/a - no input, nothing to judge |
| Ponto de entrada que não decide | `src/Api/Features/Identity/Login.cs`, `RefreshAccessToken.cs` (the endpoints, "instrumentação sobre handlers já provados") | one at the boundary | yes - accepted entry C1/C2 (`IdentityAuthE2ETests.cs:71,89`), rejected entry C4 (`:117`), rotation path C3 (`:104-106`) |
| Instrumentação, pass-through | `src/web/src/app/core/http/api.interceptor.ts` (adds `withCredentials`) | none of its own | yes - covered by the consumer proofs C8 (`api.interceptor.spec.ts:60`) and C12 (`e2e/auth.spec.ts:20`) |

## Faults injected

Isolation was **backup-and-restore in the real tree, not a `git worktree`**: `HEAD` (`902d206`) predates
the entire feature, so `git worktree add <scratch> HEAD` would have produced a tree with none of the
code under test. Each file was copied to `/tmp/verifier-b-backup/` before mutation, restored from that
copy afterwards (md5 equal in all five cases), and `git status --porcelain` was diffed against the
baseline captured before the first mutation - identical, byte for byte. Every proof was re-run green
after restore.

| Mutation | Location | Proof run | Killed |
| --- | --- | --- | --- |
| `HttpOnly = true` -> `HttpOnly = false` | `src/Api/Features/Identity/RefreshCookie.cs:19` | C1 `Login_ShouldSetHttpOnlyRefreshCookie` | yes - `Assert.Contains() Failure: Sub-string not found` |
| no-cookie branch `return false` -> `return true` | `src/Api/Features/Identity/Logout.cs:19` | C6 `Handle_ShouldNoOp_WhenTokenIsMissing` | yes - `Assert.False() Failure` |
| missing-cookie throw `UnauthorizedAccessException` -> `NotFoundException` (401 becomes 404) | `src/Api/Features/Identity/RefreshAccessToken.cs:110` | C4 `Refresh_WithoutCookie_ShouldReturn401` | yes - `Assert.Equal() Failure: Values differ` |
| `withCredentials: true` -> `withCredentials: false` | `src/web/src/app/core/http/api.interceptor.ts:53` | C8 `envia withCredentials` | yes - `AssertionError: expected false to be true` |
| `apply()` also writes `token: token.accessToken` into `pt.auth` (`StoredAuth` at `:19-22`, `apply` at `:67-77`) | `src/web/src/app/core/session/session.store.ts:70` | C12 `renova o token expirado` (Playwright, real API) | yes - `expect(received).not.toContain("token")`, with the JWT visible in the received `localStorage` dump |

Five faults on five distinct assertion surfaces, each forcing a different proof to fail, across three
runners plus a real browser. The C12 kill also settles that the Playwright proof genuinely rebuilds
and serves the current tree rather than reusing a stale bundle.

## Walkthrough with the user

**Not run** - no channel to the user is available in this execution. Step 5 did not run; it is not
recorded as passed. The user-facing surface this feature touches is one interaction (`Sair` now
revokes before clearing) and one invisible property (no token in web storage), both of which have
automated coverage, but human judgment on the flow has not been obtained.

## Findings

Ranked.

1. **C17 has no test.** `SolutionFileTests.Solution_ShouldListEveryProject` does not exist; the filter matches nothing and exits `0`. `checks.md:68` - no `file:line`.
2. **C18 has no test.** `SolutionFileTests.ProjectGuids_ShouldMatch_TemplateConfig` does not exist; same silent-zero. `checks.md:71` - no `file:line`.
3. **C6 level gap.** The claim names `204`; the sole proof asserts a handler bool at `tests/Api.Tests/Identity/LogoutTests.cs:24`. `checks.md:90` asserts C6 "tem uma prova que atravessa a fronteira HTTP" - it does not.
4. **Five front proof commands are unrunnable as written.** `npx vitest run <file> -t "<name>"` (C8, C9, C10, C11, C16) fails with `ReferenceError: localStorage is not defined` at `src/web/src/test-setup.ts:17` - there is no vitest config; `angular.json` `architect.test` supplies the jsdom environment, `providersFile` and `setupFiles` through `@angular/build:unit-test`. The runner that works is `npx ng test --no-watch --include <file>`. Confirms the earlier Verifier's report; all five pass under the real runner.
5. **C7 level gap.** The claim names a `Set-Cookie` attribute; the only proof reads `CookieOptions.Secure` (`LogoutTests.cs:62`). No test observes `secure` on a real header in either direction, and the `Test policy` row for `RefreshCookie.cs` demands a boundary proof for the decision.
6. **Logout's `429` is unproven and miscredited.** `checks.md:83` credits `web-frontend` C13, which stubs a `429` on `/login` in MSW. The policy is wired (`Logout.cs:55`), but nothing proves the new route emits `429`.
7. **The Coverage doors row claims a proof for a superseded door.** `checks.md:85` lists "documento no build C13" alongside "documento versionado C13"; the plan's `Landing` supersedes the first, nothing ships it, and the cited test `OpenApiContractTests.Document_ShouldBeGenerated_AtBuildTime:89` asserts route counts - it is named after the rejected shape.
8. **`Swept` row "dependency failure" cites the superseded door.** `checks.md:125` says "o documento OpenAPI é gerado pelo próprio build". It is generated by the test host (`OpenApiDocumentTests.cs:39-50`), not the build. An `n/a` policy row, so no code is wrong - the stated reason is.
9. **`Surface` names `429` on three routes that `openapi.json` does not declare**, and the drift guards compare paths and methods only (`OpenApiContractTests.cs:53-58`), so a status drift passes both directions of the guard.
10. **The cancelled-confirm branch of `Sair` has no check.** `shell.spec.ts:36` only runs `stubConfirm(true)`; the repo's own convention covers the analogous case (`web-frontend` C24, "cancelar nao emite requisicao").
11. **C11 precision gap.** "antes de limpar o estado local" is unasserted - `shell.spec.ts:48-51` would pass if the clear ran first.
12. **C1 precision gap.** `Landing` fixes `Max-Age=<dias*86400>`; `IdentityAuthE2ETests.cs:76` asserts only `Assert.Contains("max-age=", …)`.
13. **C10 precision gap.** "emitido sem corpo com token" is unasserted; the empty body lives at `refresh-coordinator.ts:29`.
14. **`docs/security/RBAC_MATRIX.md` is an `Observable` row with no check.** The doc was updated correctly (`RBAC_MATRIX.md:31`), but nothing would catch a regression.

## What the security property actually proves

The load-bearing claim - the browser cannot read the refresh token - is proven end to end, not by
assertion about server config alone. `src/web/e2e/auth.spec.ts:30-32` runs a real browser against a
real API and asserts the token appears in neither `localStorage`, nor `sessionStorage`, nor
`document.cookie`; the mutant that wrote a token into `pt.auth` was killed by it. Revocation is
proven at the boundary: `IdentityAuthE2ETests.cs:136-138` replays the exact cookie after `logout` and
gets `401`. Both hold.

## Gate

`python3 scripts/validate_verification.py auth-cookie-contract --root /Users/luissoares/Repos/Feature.template` - exit 1, 1 error (verdict is FAIL), 0 warnings.

Proof runs behind this report:

- `dotnet test tests/E2ETests --no-build --filter "…"` - 6 tests, Aprovados: 6
- `dotnet test tests/Api.Tests --no-build --filter "…LogoutTests|…RefreshCookieTests"` - 4 tests, Aprovados: 4
- `dotnet test tests/ArchitectureTests --no-build --filter "…OpenApiContractTests"` - 3 tests, Aprovados: 3
- `npx ng test --no-watch --reporters=verbose --include …` (4 spec files) - 21 passed, 0 failed
- `PW_CHANNEL=chrome npx playwright test e2e/auth.spec.ts -g "renova o token expirado"` - 1 passed
- `dotnet build Product.Template.sln` - 0 Erro(s), four projects
- `dotnet test tests/ArchitectureTests --filter "FullyQualifiedName~SolutionFileTests.Solution_ShouldListEveryProject"` - **Nenhum teste corresponde ao filtro**, exit 0
